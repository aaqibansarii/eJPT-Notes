# Website Recon & Footprinting
# Passive Recon
IP addresses
Directories hidden from search
Names
Email addresses
Phone Numbers
Physical Addresses
Web technologies being used

## Tools
```bash
#Gives ipv4 & ipv6 for provided url
host <targeturl>

#Look for "robots.txt" to see allowed and disallowed files given by the site provider

Ex:
User-agent: *
Disallow:/wp-admin/
Allow: /wp-admin/admin-ajax. php
Disallow: /wp-content/uploads/wpo-plugins-tables-list.json

# same as the above we have
sitemap.xml
wp-config.bak
# use browser plugins
built with # Tells you whats running on the website
wappalyzer # identifies technology of the webiste
whatweb
httrack # suppose u want to downlaod the source code of site so use:

crt.sh
```

## whois enumeration
```bash
# allows users to retrieve information about domain names, IP addresses, and network devices. 
# when the site was updated

whois
who.is
# The "Whois" lookup utility can be used to identify the nameservers of a particular domain.
```

## Website Footprinting With Netcraft
```bash
netcraft.com
# ip
# ssl validity
# many more 
```


## DNS Recon
```bash
#dnsrecon
DNSRecon is a Python script that provides the ability to perform: Check all NS Records for Zone Transfers. Enumerate General DNS Records for a given Domain (MX, SOA, NS, A,AAAA, SPF and TXT). Perform common SRV Record Enumeration. Top Level Domain (TLD) Expansion.

1. MX: Specifies the mail server for a domain.
2. SOA: Contains administrative info about the domain.
3. NS: Lists authoritative name servers for a domain.
4. A: Maps a domain to its IPv4 address.
5. AAAA: Maps a domain to its IPv6 address.
6. SPF: Defines allowed mail servers for a domain.
7. TXT: Stores arbitrary text, often for verification.

# dnsdumpster.com
https://dnsdumpster.com/
DNSDumpster.com is a FREE domain research tool that can discover hosts related to a domain. Finding visible hosts from the attackers perspective is an important part of the security assessment process.
```

## WAF With wafwOOf
```bash
WAFW00F 
# can detect a number of firewalls
# identify which firewall the target is using to strategize the attack.
```

## Subdomain Enumeration With Sublist3r
| Short Form | Long Form            | Description                                         |
|------------|----------------------|-----------------------------------------------------|
| -d         | --domain             | Domain name to enumerate subdomains of              |
| -b         | --bruteforce         | Enable the subbrute bruteforce module               |
| -p         | --ports              | Scan the found subdomains against specific TCP ports |
| -v         | --verbose            | Enable verbose mode and display results in real-time |
| -t         | --threads            | Number of threads to use for subbrute bruteforce    |
| -e         | --engines            | Specify a comma-separated list of search engines     |
| -o         | --output             | Save the results to a text file                      |
| -h         | --help               | Show the help message and exit                       |

```bash
sublist3r -d <url>
subfinder
assestfinder
```

## Google dorks
```bash
site:
filetype:-www -store # To remove words from urls 
inurl:
site:*.sitename.com
intitle:
intitle:index of
cache:target.com # to look the old version of website same as waybackurls
inurl:auth_user_file.txt
inurl:passwd.txt
inurl:wp-config.bak

# waybackurls 
search for usefull info to use in latest webiste 

# google hacking databse
to see search querries
```

## Email Harvesting With theHarvester
```bash
theHarvester -d hackersploit.org -b bing,otx,urlscans
theHarvester -d HackerSploit -b bing,otx,urlscans # using companyname
# none the above worked for me

# info like email found here can be used for phishing thus it is a flaw

you can also use "spyse" that is very good but paid
```

## Leaked Password Databases
```bash
haveibeenpwnd # just search the email there in site
#use this to get the sites on which the above found mail was used to login
```

---
---

# Active Recon

## DNS Records
```typescript
"these are the records u mostly found on dns."
DNS Records
A - Resolves a hostname or domain to an IPv4 address.
AAAA - Resolves a hostname or domain to an IPv6 address.
NS - Reference to the domains nameserver.
MX - Resolves a domain to a mail server.
CNAME - Used for domain aliases.
TXT - Text record.
HINFO - Host information.
SOA - Domain authority.
SRV - Service records.
PTR - Resolves an IP address to a hostname
```

## DNS Interrogation
```typescript
DNS interrogation is the process of enumerating DNS records for a specific domain.
The objective of DNS interrogation is to probe a DNS server to provide us with DNS records for a specific domain.
This process can provide with important information like the IP address of a domain, subdomains, mail server addresses etc.
```

#### Tools
```bash
dnsrecon # this gives less info about dns
dnsdumpster.com # same less info

dnsenum # active/passive both  "prefered"
> dnsenum <targeturl> #usage

dig
> "dig axfr example.com @ns1.example.com"
1. **`dig`**: Tool for querying DNS servers.
2. **`axfr`**: Requests a zone transfer to retrieve all DNS records.
3. **`example.com`**: Target domain for the zone transfer.
4. **`@ns1.example.com`**: Specifies the DNS server to query.

#NOTE: - A successful transfer indicates a misconfigured DNS server, exposing sensitive information. Proper restrictions should be in place to prevent unauthorized access.

fierce "Not workinf properly for me"
```

## Host discovery
```bash
> ip a s

nmap
> sudo nmap -sn 192.168.2.0/24 # -sn checks for active hosts without port scanning.

netdisover 
> netdiscover -i eth0 -r 192.168.2.0/24
```

## Port scanning (nmap)
```bash
nmap <ip> # scan default 1000 ports

nmap -Pn 10.4.19.218 # -Pn to by pass blocking by windows sys as it dont take icmp ping probes

nmap -Pn -p- 10.4.19.218 # -p- to scan all range of ports

nmap -Pn -p 80,443,22 10.4.19.218 # -p specific port scan

"filetered" in output means there might be firewall on the port

nmap -Pn -p1-1000 10.4.19.218 # scan a range of ports "1-1000 here"

nmap -Pn -sU 10.4.19.218 # -sU udp scan

nmap -Pn -F 10.4.19.218 # -F fast scan

nmap -Pn -F -sV 10.4.19.218 # -sV version scan

nmap -Pn -F -sV -O 10.4.19.218 # operating system scan

nmap -Pn -F -sV -O -sC 10.4.19.218 # script scan

nmap -Pn -F -sV 10.4.19.218 -v # to see the status of scan live

nmap -Pn -A 10.4.19.218 # includes nmap -Pn -sV -O --script=default --traceroute 10.4.19.218

################################


```

### Importance of nmap

| Option | Name       | Description                                         |
|--------|-----------|-------------------------------------------------------|
| **-T0** | Paranoid   | Very slow; avoids detection.                         |
| **-T1** | Sneaky     | Slow; reduces chance of triggering alarms.           |
| **-T2** | Polite     | Slower than normal; minimizes network impact.        |
| **-T3** | Normal     | Default speed; balanced for most scans.              |
| **-T4** | Aggressive | Faster; may lead to detection.                      |
| **-T5** | Insane     | Very fast; likely to overwhelm and be detected.     |

# Methodoloy
```bash
# get the ip
> host <targeturl>
# get info on the target
> whois <targetip>
# use google dorks to see the sensitive data that should not be available to normal people
such as wordpress specific data of that site
```

# ctf challange
https://medium.com/@prinuguptaq0/assessment-methodologies-information-gathering-ctf-1-ejpt-ine-f9498ba88d61